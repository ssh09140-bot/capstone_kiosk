EXPO_PUBLIC_API_URL=http://localhost:3000/api
# For testing on a physical device or Android emulator, you might need to replace 'localhost' with your machine's local IP address (e.g., 192.168.1.X).
# For Android emulator, '10.0.2.2' can often be used to refer to the host machine's localhost.